export enum TrxStorage {
  cache = 'cache',
  chain = 'chain',
}

export enum TrxStatus {
  replaced = 'replaced',
  normal = 'normal',
  deleted = 'deleted',
}

export enum TrxType {
  post = 'post',
  comment = 'comment',
  profile = 'profile',
  counter = 'counter',
  discounter = 'discounter',
  image = 'image',
  groupInfo = 'group_info',
}
